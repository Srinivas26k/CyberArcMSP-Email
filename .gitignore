# Python-generated files
__pycache__/
*.py[oc]
dist/
wheels/
*.egg-info

# Electron build OUTPUT (keep the build/ RESOURCES folder tracked)
dist/

# Virtual environments
.venv
# Bundled standalone Python runtime (created by CI, never committed)
bundled-python/

.env

# Build+send script credentials (NEVER commit)
scripts/.build_send.env

node_modules
database.db
.env

tests
# *.md